# モジュール分割・資料更新の確認記録

確認日: 2026-09-13。

| 対象 | 結果 |
| --- | --- |
| ソース照合 | 画面部品、グラフ計算、プロフィールの型・検証・変換、RustのI/Oと起動を分割。既存の未コミット変更を引き継いで実施 |
| npm test | 成功。186項目の整合性、SSR、プロフィール・バイナリ・IPCモック検証 |
| npm run build | 成功。TypeScript 型チェックと Vite 本番ビルド |
| cargo test -p skilltopo --locked | 8テスト成功 |
| npm run format:check / cargo fmt --all -- --check | 成功。TypeScript・TSX・CSS・テスト・Rustの整形を確認 |
| 画面描画の比較 | 分割前後の全6画面をSSRで比較。整形によるReactのテキスト境界コメントを除いてHTMLが一致 |
| 図 | 既存6図を維持。構成図のファイル群のラベルを .puml / .svg の両方で同期 |
| ドキュメントリンク | 引継ぎ資料内の相対ファイルリンクと埋め込み画像の存在を検証 |
| git diff --check | 成功 |

今回はコードの分割・日本語コメント追加・整形と、引継ぎ資料の更新を行いました。保存形式・カタログデータ・画面の操作仕様を維持する方針です。アプリの実行依存の追加はなく、開発時の整形用にPrettierを追加しました。

SSR比較は初期データを使い、描画確認用に起動待ちを解除して各画面を表示したものです。クリックや入力、Reactのeffect、ネイティブダイアログ、表示サイズごとのレイアウトはこの比較の対象外です。比較用の一時ファイルはGit管理外の `artifacts/` に置いており、通常の `npm test` の依存ではありません。

今回デスクトップの全操作を再度手動確認したわけではなく、配布用exeも生成していません。配布前には [修正ガイド](maintenance.md) のビルド・手動確認を実施してください。

PlantUMLの正本は `diagrams/*.puml`、閲覧用は同名 `.svg` です。今回の構成図は配置を維持してラベルを同期しました。過去のPNGは分割前の確認用であり、今回の資料には使いません。
